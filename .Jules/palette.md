# Palette's Journal

## 2024-10-24 - Duplicated Header Architecture
**Learning:** The website architecture relies on duplicated HTML header and navigation structures across five files (`index.html`, `games.html`, `about.html`, `space-shooter.html`, `tetris.html`), requiring manual synchronization for global changes.
**Action:** Always check and update all HTML files when modifying the header or navigation to ensure consistency.
